# Knowledge Base

Place documents, FAQs, or other reference material here that the local AirLLM
model can use as context for reasoning or content generation. Agents may load
and query this data to provide more informed responses.

Files can be plain text, markdown, or JSON. You may implement loaders in
`ai_model/model_loader.py` to ingest this content.
